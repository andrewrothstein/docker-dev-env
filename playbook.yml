---
- hosts: localhost
  tasks:
    - name: fetching upgrades...
      shell: apt-get update -y && apt-get upgrade -y
- hosts: localhost
  roles:
    - andrewrothstein.git
    - andrewrothstein.hub
    - andrewrothstein.corkscrew
    - andrewrothstein.nettools
    - andrewrothstein.emacs
    - spk83.dumb-init
    - andrewrothstein.sshd-supervisord
